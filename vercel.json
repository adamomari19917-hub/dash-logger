{
    "version": 2,
    "builds": [
        {
            "src": "server/main.js",
            "use": "@vercel/node"
        },
        {
            "src": "client/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "build"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/server/main.js"
        },
        {
            "src": "/(.*)",
            "dest": "/client/$1"
        }
    ]
}